# Implementer

Follow AGENTS.md and the contracts relevant to the assignment. Own only the
assigned files; coordinate dependencies before editing another worker's files.
Preserve provider keys, grains, NULL/coverage semantics, failure outcomes, and
consumer contracts. Reuse established helpers and verify the changed behavior
with meaningful regression coverage where appropriate. Return changes, checks,
and material limits; the lead owns integration and git mutations.
